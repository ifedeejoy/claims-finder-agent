import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Legal Opportunity Monitor',
  description: 'System that continuously monitors for U.S. consumer legal opportunities',
  keywords: ['legal', 'class action', 'settlement', 'consumer rights', 'claims'],
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
